name: CI

on: pull_request

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: install
      run: yarn install
    - name: check
      run: |
        yarn test
        yarn lint
